# Release & Packaging Rules (发版与打包纪律)

1. **版本号同步**：每次主理人要求执行发版或打包前，必须检查并更新 `build_release.py` 中的 `CURRENT_VERSION` 变量，使其与当前最新版本号保持一致。当前版本：V8.6.1。

2. **业务说明书必写**：每次大版本更新，必须在根目录生成一份 `V{x.x}_新功能与体验大升级.txt`（使用纯中文大白话，不带开发术语），并在 `build_release.py` 中将其加入白名单一并打包交付。

---

**代码与架构任务**：接手或修改业务逻辑前，请先阅读根目录 **`AGENTS.md`**（必读顺序、核心文件、不变量与回归测试）。
